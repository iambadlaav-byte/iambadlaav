/**
 * LoginPage — stub for Plan 06 (Auth flow).
 * Renders a placeholder so the route resolves in Phase 1.
 */
export default function LoginPage() {
  return (
    <main className="flex flex-col items-center justify-center min-h-[60vh] px-4 text-center">
      <h1 className="font-display text-3xl font-light text-charcoal mb-4">Sign in</h1>
      <p className="text-muted font-sans max-w-narrow">
        The login page ships in Plan 06.
      </p>
    </main>
  );
}
